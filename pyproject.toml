[project]
name = "chinese-srs"
version = "0.1.0"
description = "Spaced Repetition Review for Chinese Vocabulary & Grammar"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.0",
    "libsql>=0.1.11",
]
